{
  "name": "sacmed-mvp",
  "version": "0.2.0",
  "description": "MVP do SACMed - Sistema de Agendamento de Consultas Medicas",
  "private": true,
  "scripts": {
    "start": "node backend/src/server.js",
    "test": "node --test $(find backend/tests -type f -name '*.test.js')",
    "build": "bash scripts/build.sh"
  },
  "engines": {
    "node": ">=18"
  }
}
